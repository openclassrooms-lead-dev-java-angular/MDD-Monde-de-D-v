# Application and Spring configuration for development

spring:
  mail:
    properties.mail.smtp:
      auth: false
      starttls:
        enable: false

app:
  seed:
    enabled: true
  environment: DEV

logging:
  level:
    root: INFO
    com.openclassrooms.mddapi: DEBUG
    com.openclassrooms.mddapi.seeder: INFO
    com.openclassrooms.mddapi.cli: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: TRACE